# 阶段一运行说明

## 0. 关键技术与依赖版本
- Java：21
- Spring Boot：3.2.5
- Spring Cloud：2023.0.2
- Spring Cloud Alibaba：2023.0.1.0
- Nacos：2.3.0
- MySQL：8.0
- Redis：7.2
- Prometheus：2.51.2
- Grafana：10.4.1
- Sentinel Dashboard：1.8.7

## 1. 启动前置服务（必须）
启动项目之前必须先启动以下基础服务：
- Nacos（注册中心+配置中心）：`8848`
- MySQL（认证与权限数据库）：`3306`
- Redis（缓存与 Token 黑名单预留）：`6379`

可选服务（不影响服务启动，但影响监控与治理验证）：
- Sentinel Dashboard：`8858`
- Prometheus：`9090`
- Grafana：`3000`

## 2. 基础依赖启动
```bash
docker compose up -d
```

## 3. 本地启动顺序（建议）
```bash
mvn -q -pl gateway-service,auth-service,permission-service -am -DskipTests package

mvn -pl gateway-service spring-boot:run
mvn -pl auth-service spring-boot:run
mvn -pl permission-service spring-boot:run
```

## 3.1 使用 .env 加载环境变量（推荐）
如果你使用 `.env` 统一管理地址，可在启动前执行：
```bash
set -a
source .env
set +a
```
说明：Swagger 地址会读取 `SWAGGER_HOST` 并在启动日志中输出。

## 4. 默认端口
- 网关：8080
- 认证服务：8081
- 权限服务：8082
- Nacos：8848
- Sentinel Dashboard：8858
- Prometheus：9090
- Grafana：3000

## 5. 常用地址
- Nacos 控制台：`http://192.168.200.128:8848/nacos`
- Sentinel Dashboard：`http://192.168.200.128:8858`
- Prometheus：`http://192.168.200.128:9090`
- Grafana：`http://192.168.200.128:3000`
- 认证服务 Swagger：`http://localhost:8081/swagger-ui/index.html`
- 权限服务 Swagger：`http://localhost:8082/swagger-ui/index.html`

## 6. 注意事项
- 当前阶段只提供服务骨架与基础依赖接入，业务能力在阶段二完善。
- Prometheus 默认抓取宿主机服务，若服务容器化需调整 `docker/prometheus.yml`。
- 数据库默认创建 `easy_auth`，`easy_permission` 可手动创建或在后续脚本中补齐。

# 企业级后端通用基础框架需求文档

## 1. 项目结构与技术栈分析
- **语言与框架**：Java 21 + Spring Boot 3（偏向云原生与企业级最佳实践）。
- **数据与缓存**：MySQL + Redis。
- **部署形态**：Docker 优先，后续平滑扩展到 Kubernetes。
- **服务形态**：微服务架构，支持服务注册、配置、网关与治理能力。
- **参考与对标**：以 GitHub 若依框架为参考，保持“开箱即用 + 组件可插拔”的工程体验。

## 2. 业务目标与核心流程
### 2.1 业务目标
- 提供一个可复用的企业级后端基础框架，用于快速搭建不同类型业务系统。
- 降低重复造轮子的成本，提升团队交付速度与系统一致性。
- 兼顾“开箱即用”和“可插拔扩展”，适配从中小项目到复杂业务系统的演进。

### 2.2 核心流程
- **项目初始化流程**：初始化服务模板 → 配置中心 → 启动基础能力（认证鉴权、RBAC、审计）。
- **业务扩展流程**：选择/安装业务模块 → 接口对接 → 自动接入监控与审计 → 统一网关暴露。
- **运维交付流程**：容器化构建 → 运行时观测 → 灰度/滚动升级。

## 3. 关键模块与高风险点
### 3.1 关键模块
- 认证鉴权与 RBAC 权限模型
- 审计日志与合规追踪
- API 网关与统一流量入口
- 监控告警与可观测性
- 限流熔断与服务治理
- 服务注册发现与配置管理
- 多租户/多组织扩展能力

### 3.2 高风险点
- **权限模型复杂度**：RBAC 设计不当易导致权限膨胀或绕过。
- **网关与鉴权耦合**：网关层策略与服务内鉴权可能冲突。
- **监控体系不统一**：多服务指标口径不一致会影响排障效率。
- **服务依赖复杂**：微服务间依赖与版本兼容引发治理成本。
- **可插拔机制失衡**：过度可插拔导致使用成本升高，过度“开箱即用”导致难扩展。

## 4. 基于项目特点的最佳实践建议
- **分层清晰**：公共能力服务与业务服务隔离，避免“上层业务侵入底层基础”。
- **配置外置**：全链路支持配置中心与环境隔离。
- **统一规范**：接口返回、异常码、日志结构、指标命名统一。
- **可观测性内建**：日志、指标、链路追踪在框架级实现。
- **模块化发布**：基础能力以 starter 或独立服务方式提供，支持按需启用。

---

## 5. 需求范围
### 5.1 必做范围（MVP）
- 用户注册登录
- 认证鉴权（JWT/OAuth2 可选）
- RBAC 权限模型
- 审计日志
- 统一 API 网关
- 基础监控与告警
- 基础限流熔断
- 接口文档（Swagger）

### 5.2 可扩展范围
- 多租户能力
- 分布式任务调度
- 消息队列与事件总线
- 配置中心与灰度发布
- API 文档与 SDK 自动生成
- 统一文件/对象存储接入

## 6. 功能性需求
### 6.1 用户与权限
- 用户注册/登录/注销/找回密码
- 组织/角色/权限管理
- RBAC 授权粒度支持到菜单、接口与数据级别

### 6.2 认证鉴权
- Token 体系（JWT/Session 可切换）
- 支持单点登录（后续扩展）
- 接口级鉴权与权限校验

### 6.3 审计与日志
- 关键操作全量审计
- 审计日志可查询、可导出
- 日志结构化与可追踪

### 6.4 网关与治理
- 统一路由与鉴权入口
- 请求限流/熔断/降级策略
- 黑白名单与 IP 管控

### 6.5 监控与告警
- 服务健康检查
- 核心指标采集（QPS、延迟、错误率）
- 告警规则可配置

## 7. 非功能性需求
- **性能**：单服务可支撑中等并发，关键接口需低延迟。
- **安全**：密码加密存储，接口防护，审计可追溯。
- **可维护性**：模块化、文档化、规范化。
- **可扩展性**：支持新增业务模块与微服务。
- **可靠性**：关键模块可降级，故障不扩散。

## 8. 体系结构与模块划分
### 8.1 总体架构
微服务架构 + 统一网关 + 公共基础能力服务。

### 8.2 模块建议
- **网关服务**：统一入口、流量管理、协议转换。
- **认证服务**：用户、Token、登录态管理。
- **权限服务**：RBAC 与权限校验策略。
- **审计服务**：操作日志与安全审计。
- **监控服务**：指标与告警收敛。
- **配置服务**：配置中心与环境隔离（可选）。

## 9. 数据模型与持久化
### 9.1 核心表设计方向
- 用户、角色、权限、组织、审计日志、配置项。
### 9.2 Redis 使用场景
- Token 缓存、验证码、热点权限缓存、限流计数器。

## 10. 部署与运维
### 10.1 Docker 交付
- 每个服务独立镜像
- 统一健康检查接口
### 10.2 K8s 扩展
- 无状态服务优先
- 支持水平扩容

## 11. 开箱即用与可插拔机制
- **开箱即用**：默认启用核心能力（认证、权限、审计、监控）。
- **可插拔**：通过模块开关或依赖裁剪，按需启用扩展模块。
- **扩展方式**：插件式 Starter 或独立服务解耦。

## 12. 与若依框架对标
- 借鉴其权限模型与模块拆分方式
- 在微服务与云原生能力上增强
- 更强调组件化与可插拔扩展

## 13. 里程碑与交付计划（建议）
1. **需求冻结**：确认核心模块与边界  
2. **架构设计**：模块划分与服务接口定义  
3. **基础能力交付**：认证、RBAC、审计、网关  
4. **治理能力交付**：监控、告警、限流熔断  
5. **扩展能力交付**：多租户、任务、消息等  

## 14. 风险与对策
- **权限模型过复杂**：先采用简化 RBAC，后续渐进扩展。
- **运维复杂度提升**：提供本地一键启动与 Docker Compose。
- **组件耦合过深**：通过接口/Starter 解耦，避免直接依赖内部实现。

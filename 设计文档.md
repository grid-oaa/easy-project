# 企业级后端通用基础框架设计文档

## 1. 设计核心目标
- **开箱即用**：默认提供认证鉴权、RBAC、审计日志、网关、监控告警、限流熔断等核心能力。
- **可插拔扩展**：通过模块开关与独立服务/Starter 方式按需启用或替换组件。
- **微服务可演进**：支持从少量服务起步到多服务规模化演进。
- **可观测性内建**：日志、指标、链路追踪统一标准与接入方式。
- **对标若依**：参考其菜单/功能划分，保留必要部分，避免冗余。

## 2. 设计架构图
```
                     ┌────────────────────────────────┐
                     │           外部调用方           │
                     └────────────────────────────────┘
                                     │
                                     ▼
                           ┌─────────────────┐
                           │ API 网关服务     │
                           │ Spring Cloud GW │
                           └─────────────────┘
                                     │
                   ┌─────────────────┼──────────────────┐
                   ▼                 ▼                  ▼
        ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
        │ 认证服务         │  │ 权限服务         │  │ 业务服务集群     │
        │ OAuth2.1+JWT     │  │ RBAC/菜单/接口   │  │ 可扩展/按需拆分  │
        └─────────────────┘  └─────────────────┘  └─────────────────┘
                   │                 │                  │
                   └─────────────────┼──────────────────┘
                                     ▼
                           ┌─────────────────┐
                           │ 审计日志服务     │
                           └─────────────────┘

          ┌─────────────────────────────────────────────────────┐
          │ 基础设施层                                           │
          │ Nacos(注册+配置) | MySQL | Redis | Kafka            │
          │ Sentinel(限流熔断) | Prometheus+Grafana | SkyWalking │
          └─────────────────────────────────────────────────────┘
```

## 3. 模块划分
### 3.1 网关服务
**定位**：统一入口、路由转发、基础安全与流量治理。
- **子功能**
  - 路由与服务发现集成（Nacos）
  - Token 校验与鉴权拦截
  - 全局限流、黑白名单、基础防护
  - 登录态失效与访问令牌黑名单
  - 统一错误码与响应格式
  - 网关级审计日志上报

### 3.2 认证服务
**定位**：统一用户认证与令牌签发。
- **子功能**
  - 用户注册/登录/注销
  - OAuth2.1 授权流程
  - JWT 生成与刷新
  - 多端登录策略与会话管理

### 3.3 权限服务
**定位**：RBAC 权限模型与资源授权。
- **子功能**
  - 组织/角色/用户/权限管理
  - 菜单与功能点授权（参考若依必要菜单）
  - 接口级权限校验
  - 权限资源批量导入
  - 数据权限策略（基础版本）

### 3.4 审计日志服务（后续增强）
**定位**：先预留审计能力的接口与标准，当前阶段不作为重点交付。
- **子功能**
  - 审计事件采集接口（预留）
  - 审计日志存储与查询（后续完善）
  - 风险操作标记与告警触发（后续完善）

### 3.5 业务服务集群
**定位**：承载具体业务逻辑，可横向扩展。
- **子功能**
  - 标准化接口与错误码
  - 接入统一鉴权与审计
  - 按需拆分为多个独立服务

### 3.6 监控与告警
**定位**：运行状态与性能可观测。
- **子功能**
  - 指标采集（Prometheus）
  - 可视化面板（Grafana）
  - 告警规则管理
  - 服务健康状态汇总

### 3.7 接口文档与开发支持
**定位**：统一接口说明与调试入口。
- **子功能**
  - Swagger UI 统一入口
  - OpenAPI 文档生成
  - 接口调试与示例展示

### 3.8 限流熔断与服务治理
**定位**：流量保护与稳定性保障。
- **子功能**
  - Sentinel 限流、熔断、降级
  - 规则配置与动态下发
  - 关键链路保护

### 3.9 基础设施层
**定位**：统一的存储、缓存、消息与配置能力。
- **子功能**
  - Nacos 注册与配置中心
  - MySQL 持久化存储
  - Redis 缓存与分布式锁
  - Kafka 事件总线

## 4. 模块功能交互
- **登录流程**：客户端 → 网关 → 认证服务 → 颁发 JWT → 网关透传 Token。
- **访问受限接口**：客户端 → 网关校验 Token → 权限服务校验权限 → 业务服务处理。
- **审计流程（后续增强）**：网关与业务服务产生审计事件 → 审计服务落库 → 可查询与导出。
- **限流熔断**：网关或业务服务调用 Sentinel → 命中策略则降级或拒绝。
- **监控采集**：业务与基础服务暴露指标 → Prometheus 抓取 → Grafana 展示。
- **配置变更**：配置中心更新 → 服务动态刷新配置。

## 5. 使用到的技术栈
### 5.1 后端与框架
- Java 17
- Maven 3.9.9
- Spring Boot 3
- Spring Cloud 相关组件

### 5.2 网关与治理
- Spring Cloud Gateway
- Sentinel（限流/熔断/降级）

### 5.3 认证与安全
- OAuth2.1
- JWT

### 5.4 注册与配置
- Nacos（注册中心 + 配置中心）

### 5.5 存储与缓存
- MySQL
- Redis

### 5.6 数据访问
- MyBatis-Plus

### 5.7 监控与链路追踪
- Prometheus
- Grafana
- Apache SkyWalking

### 5.8 接口文档
- Swagger（OpenAPI）

### 5.7 消息与事件
- Kafka（可插拔）

## 6. 若依功能参考与保留范围
- **保留**：用户管理、角色管理、菜单管理、权限管理、日志审计、配置管理。
- **精简**：与业务强绑定的示例模块不纳入基础框架。
- **扩展**：在微服务与可观测性方面增强。

## 7. RBAC 接口级权限设计
### 7.1 授权粒度
- **接口级权限**：以“服务名 + API 路径 + HTTP 方法”为唯一资源标识。
- **菜单权限**：仅用于前端展示与导航控制，不作为后端授权依据。
- **数据权限**：当前阶段不展开，预留扩展点。

### 7.2 权限校验流程
- 网关完成 Token 基础校验（签名、过期、黑名单）。
- 权限服务负责接口级权限判定，返回是否允许访问。
- 业务服务再做必要的业务级校验与防护。

### 7.3 权限模型要点
- 角色绑定接口权限列表（可按模块批量赋权）。
- 支持“只读/读写/管理”三档常用权限模板，降低配置成本。
- 权限变更支持实时生效（结合 Redis 缓存与失效策略）。

## 8. 可插拔机制设计要点
- **模块开关**：通过配置启用/禁用非核心模块。
- **Starter 化**：公共能力以 Starter 输出，降低接入成本。
- **独立服务化**：高内聚模块独立部署，按需组合。
